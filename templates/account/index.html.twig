{% extends 'base.html.twig' %}

{% block title %}Mon compte - Garage V. PARROT{% endblock %}

{% block content %}
    <div class="mt-5 p-5">
        <h1>Mon compte</h1>
        Bienvenue {{ app.user.prenom }} dans votre compte
        <hr>
        <a href="{{ path('account_password') }}">Modifier mon mot de passe</a>
        <hr>
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('admin') }}">Panneau d'administration</a>
        {% else %}
            <a href="{{ path('employee') }}">Panneau d'administration</a>
        {% endif %}

    </div>

{% endblock %}

